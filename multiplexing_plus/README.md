# multiplexing_plus/

Extension placeholder for future experiments beyond the Phase 8 toy.

Ideas:
- Flow control: per-stream receive windows (like HTTP/2 WINDOW_UPDATE)
- Priority frames: weight-based scheduling across concurrent streams
- Header compression: simple static or dynamic table
- Server push: server-initiated streams (PUSH_PROMISE)
